import { CarrierWorkspaceLayout } from '@/components/insurance/CarrierWorkspaceLayout'

export default function AseguradoraLayout({ children }: { children: React.ReactNode }) {
  return <CarrierWorkspaceLayout>{children}</CarrierWorkspaceLayout>
}
